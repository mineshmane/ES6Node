// Initialize an object
const session = {
    id: 1,
    time: `26-July-2018`,
    device: 'mobile',
    browser: 'Chrome'
};
console.log(session);
// Get all values of the object
const values = Object.values(session);

console.log(values);